---
title: "SDK 2.0.0 샘플앱 검증 — 발행 재개 지점 (2026-07-15 중단)"
type: work-log-resume
repo: inavi-navigation-demo-android
description: "SDK 2.0.0 검증 완료·릴리스노트 작성 완료. 다음 주 '발행해' 한마디로 커밋+푸시+gh-pages 확인만 남음."
related:
  - repo: inavi-navigation-demo-android
    path: docs/work-order-sdk-verify-and-publish-guide.md
    note: 이 검증의 근거 작업지시서
  - repo: navisdk_aos
    path: docs/2026-07-15-flutter-to-develop-cherry-pick.md
    note: SDK 2.0.0 배포(jfrog) 실행 기록
---

# SDK 2.0.0 샘플앱 검증 — 발행 재개 지점

> **2026-07-15 여기까지 작업. 다음 주 "발행해" 한마디면 아래 [발행 절차]만 실행하면 끝.**

## 완료된 것 ✅
- **대상 버전**: 2.0.0 (artifactory latest, jfrog 발행 완료)
- **샘플앱 검증**: `app/build.gradle` → `inavi-navigation-sdk:2.0.0` 교체, `assembleDebug` 성공, 에뮬레이터 구동, **init 성공 + 지도 정상 표출** 확인.
  - (참고: 검증 중 1003 인증오류 발생했으나 **서버측 일시 문제**로 판명, 이후 정상. 코드/배포 무관.)
- **릴리스노트 작성 완료** (사용자 컨펌됨):
  ```
  ### v2.0.0 — 2026-07-15
  - 정식 버전 릴리즈
  - 내부 모듈 안정성 강화
  - 지도 엔진 최신화
  ```

## 현재 작업트리 상태 (uncommitted)
| 파일 | 변경 | 발행 시 커밋? |
|---|---|---|
| `docs/index.md` | v2.0.0 개정이력 + 의존성 `0.9.0→2.0.0` | ✅ 커밋 |
| `README.md` | 의존성 `0.9.0→2.0.0` | ✅ 커밋 |
| `app/build.gradle` | 의존성 `0.9.1→2.0.0` | ✅ 커밋 |
| `app/src/main/AndroidManifest.xml` | appkey placeholder→실제키 | ❌ **커밋 금지**(public repo, 키 노출) |
| `docs/work-order-*.md`, `docs/*-resume.md`, `docs/vitepress-pages-rollout-*.md` | 내부 문서(untracked) | ❌ 커밋 안 함(내부용) |
| `local.properties` | 로컬 sdk.dir | ❌ gitignore |

## 발행 절차 ("발행해" 시 실행)
```bash
cd /Users/seungjunlee/Workspace/Projects/inavi-navigation-demo-android
git checkout master && git pull --ff-only

# ⚠️ manifest 실제키 커밋 금지 → 발행 전 placeholder로 원복
git checkout -- app/src/main/AndroidManifest.xml

# 4. 커밋 대상 3파일만 (내부 문서 add 금지)
git add README.md docs/index.md app/build.gradle
git commit -m "docs: SDK 2.0.0 검증 및 개발자 가이드 갱신"
git push origin master

# 5. gh-pages 반영 확인
gh run watch $(gh run list --workflow deploy-docs.yml -L 1 --json databaseId -q '.[0].databaseId')
sleep 90
curl -s https://inavi-systems.github.io/inavi-navigation-demo-android/android-developer-guide/ | grep -o "2.0.0" | head -1   # (SPA라 안 잡힐 수 있음 → 브라우저로 개정이력 v2.0.0 확인)
```

## 남은 체크
- [ ] manifest placeholder 원복 확인 후 3파일 커밋
- [ ] master push
- [ ] GitHub Actions(`deploy-docs.yml`) success
- [ ] 라이브 가이드에 v2.0.0 노출
- [ ] (별도) Flutter plugin example 앱 검증 — `inavi_navisdk_flutter_plugin`
